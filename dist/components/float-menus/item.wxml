<block>
    <button class="{{'float-menu__item ' + (hide ? 'hidden' : '')}}" bindtap="funPrivateusqOA" open-type="{{openType}}">
        <view class="{{iconPrefixClass + ' ' + iconPrefixClass + '-' + icon}}"></view>
        <slot></slot>
    </button>
</block>