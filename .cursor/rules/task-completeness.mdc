---
description: 执行用户改动时必须完整落地，禁止部分迁移
alwaysApply: true
---

# Task Completeness

- 执行用户明确的改动请求时，必须一次性完整落地，不允许只改一部分。
- 涉及“迁移/拆分/独立文件”类需求时，必须同步完成：
  - 组件内 class 引用更新
  - 样式文件迁移
  - 旧样式残留清理
  - 基础自检（lint / 关键路径检查）
- 若范围不清晰，先问清再改；若已清晰，不要用“先改一版”替代最终实现。
- 禁止为省事保留隐式依赖（例如组件仍依赖旧文件里的同名样式）。
- 变更完成后，反馈中要明确说明“改了哪些文件”和“是否仍有残留依赖”。

