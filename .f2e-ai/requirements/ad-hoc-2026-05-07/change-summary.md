# change-summary

## 需求与计划摘要

本次为本地迭代，仓库内无对应 `plan.md`。改动包含两处：首页图片热区挂件在纵向布局下的图片 `mode` 修正；子包语言包中口令码/活动相关中文词条补全。

## 改动范围

| 文件 | 说明 |
|------|------|
| `src/pages/home/wgts/imghot-zone/index.jsx` | `SpImage` 的 `mode`：纵向（`isVertical`）时使用 `widthFix`，非纵向时使用 `heightFix`，与竖版/横版展示预期对齐（原逻辑相反）。 |
| `src/subpages/i18n/lang/index.json` | 新增 6 条 `zh-cn` 文案（口令码登录、活动口令提示、活动信息缺失、验证失败、请输入口令码、未找到企业）；`en` / `zh-tw` / `ar` 暂为空字符串。 |

## 关键逻辑或接口变化

- **热区图**：仅调整 `mode` 与 `isVertical` 的对应关系，无接口或数据结构变更。
- **i18n**：新增短 key（如 `gpdfye5`、`xbitbq8` 等），需在业务代码中通过相同 key 引用方可生效。

## 测试与验收情况

- 未运行自动化测试；建议在首页含「图片热区」且为纵向配置的场景下确认图片不变形、热区对齐正常。
- 建议在口令码/活动相关页面手动确认新增文案 key 与界面一致。

## 遗留问题与后续建议

- 新增词条的 `en`、`zh-tw`、`ar` 为空，若上线多语言需补译。
- 建议在后续需求中补齐 `.f2e-ai/requirements/<id>/plan.md`，便于与提交技能流程对齐。
